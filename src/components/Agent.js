import React from 'react'


const Agent = () => {
    /*
        Max 20 on screen, could ignore offliners (or everyone over X. hours offline)
        Create the container, use same container for Agent stats

        NAME
        status time     
        
    */


}

export default Agent