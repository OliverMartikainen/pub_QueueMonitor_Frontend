import React from 'react'


const Call = ( {call}) => {
    /* need the call length to update every sec? if so then manual timer needed
        but i'd still get an update every 6 sec to time...
    */

    // 1.CHANNEL      2.Calls in line from channel  3.Longest wait from channel (min:sec)
    // NAME         #       MM:SS
    // Limit to length?     #       Time color coded. Own css element that changes class. yeah.
    
}

export default Call